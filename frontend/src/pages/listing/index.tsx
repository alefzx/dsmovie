import Pagination from 'Components/Pagination';
import React from 'react';

const Listing = () => {
    return (
        <div>
           <Pagination />
        </div>
    );
};

export default Listing;